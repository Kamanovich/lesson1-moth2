const a = prompt('Введите число');
const b = prompt('Введите число');
if (a >= b) {
    alert(`${a} Больше чем ${b}`);
} else if (a <= b ) {
    alert(`${b} Больше чем ${a}`);
} else if (a = b) {
    alert('Равны')
}